// import

// action types

// action (creator)

// action API

// initialState
const initialState = {
  isLoggedIn: false
};
// reducer
function reducer(state = initialState, action: any) {
  switch (action.type) {
    default:
      return state;
  }
}

// reducer Function

// export

export default reducer;
